# 合成データ生成/judge で Claude API を teacher に使う場合のみ必要。
# `cp .env.example .env` してキーを入れる。完全オフライン(--backend local)なら不要。
ANTHROPIC_API_KEY=sk-ant-...

# 使うモデルを切り替えたいとき(任意):
# COURSE_MODEL=mlx-community/gemma-4-e2b-it-bf16
